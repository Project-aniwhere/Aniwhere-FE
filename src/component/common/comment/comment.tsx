interface CommentProps {
  userIcon?: string;
  nickName: string;
  uploadedTime: string;
  rate: number;
  image?: string;
  comment: string;
}

const Comment = ({}: CommentProps) => {};

export default Comment;
